# Database Connection URL (MySQL)
DATABASE_URL="mysql://root:password@localhost:3306/kisan_mitra"

# JWT Secret for authentication tokens
JWT_SECRET="kisan-mitra-sih26032-secure-jwt-secret-key-2026"

# In-Memory DB Fallback (Strictly development/preview only. MUST be false in production)
ALLOW_IN_MEMORY_DB_FALLBACK=false
